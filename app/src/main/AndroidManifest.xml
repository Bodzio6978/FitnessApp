<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.bodziowaty6978">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".singleton.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessApp">
        <activity android:name=".view.SettingsActivity" />
        <activity android:name=".view.introduction.IntroductionActivity" />
        <activity android:name=".view.NutritionSettings" />
        <activity android:name=".view.GoalActivity" />
        <activity android:name=".view.ProductActivity" />
        <activity
            android:name=".view.newproduct.NewActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.AddActivity" />
        <activity android:name=".view.auth.UsernameActivity" />
        <activity android:name=".view.auth.ForgotActivity" />
        <activity android:name=".view.auth.RegisterActivity" />
        <activity android:name=".view.auth.LoginActivity" />
        <activity android:name=".view.ErrorActivity" />
    </application>

</manifest>